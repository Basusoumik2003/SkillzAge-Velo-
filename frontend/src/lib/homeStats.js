// Not part of the exported InternzBee source; added only so the (out of
// scope) adminDashboard page resolves at build time.

export const DEFAULT_HOME_STATS = {
  learners: { value: 0, suffix: "+", override: false },
  projects: { value: 0, suffix: "+", override: false },
  companies: { value: 0, suffix: "+", override: false }
};
