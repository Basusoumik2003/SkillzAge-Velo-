"use client";

// Minimal stub: the original InternzBee navigation loading-bar provider was
// not part of the exported source. This preserves the import contract used
// by src/app/layout.js without adding any new behavior.
export default function NavigationLoaderProvider({ children }) {
  return children;
}
