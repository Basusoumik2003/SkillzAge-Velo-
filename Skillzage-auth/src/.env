# Server
PORT=5000
NODE_ENV=development

# PostgreSQL
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=KrishiPSQL@23
PGDATABASE=testauth

# Alternatively, a single connection string (takes precedence if set)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/skillzage_auth

# JWT
JWT_SECRET=replace_this_with_a_long_random_secret
JWT_EXPIRES_IN=1d